import { Box } from "@mui/material";
import React from "react";

const Home = () => {
    return <Box sx={{ minHeight: "calc(100vh - 4rem)", display: "flex", justifyContent: "center", alignItems: "center" }}></Box>;
};

export default Home;

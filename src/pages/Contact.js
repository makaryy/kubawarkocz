import React from "react";
import { Box } from "@mui/material";

const Contact = () => {
    return <Box sx={{ minHeight: "calc(100vh - 4rem)", display: "flex", justifyContent: "center", alignItems: "center" }}> lorem2</Box>;
};

export default Contact;
